这个文件用于总结编写代码中遇到的问题
### Bug 1：`TcpConnetion::send()` 多发了 `\0`（服务端 `src/TcpConnetion.cpp`）@cli/client.cpp

```cpp
// 修复前：发送了 msg.size() + 1 字节（多了 \0）
sockIO.writen(msg.c_str(), msg.size()+1);

// 修复后：只发送实际内容
sockIO.writen(msg.c_str(), msg.size());
```
> 后果：响应末尾多了 `\0`，客户端 `recv` 把 `\0` 当残留数据，下次请求时缓冲区混乱，服务端读到的数据异常导致连接被关闭。

### Bug 2：`TcpConnetion::receive()` 保留了 `\n` 且缓冲区过小

```cpp
// 修复前：buf[1024] 可能不够，且 readline 返回的字符串含 \n
char buf[1024];
sockIO.readline(buf);
return string(buf);  // "你好\n"

// 修复后：够大的缓冲区 + 去掉末尾 \n
char buf[65536] = {0};
sockIO.readline(buf);
string result(buf);
if (!result.empty() && result.back() == '\n') {
    result.pop_back();
}
return result;  // "你好"
```

> 后果：`msgTask` 中 `DictProducer::find("你好\n")` 带换行符，查找失败导致没有响应输出，线程池任务异常后连接关闭。

### Bug 3：客户端 `receiveResponse()` 超时时未关闭 socket（`cli/client.cpp`）

> 超时/断开时直接 `_sockfd = -1` 造成 fd 泄漏。修复为 `close(_sockfd); _sockfd = -1;`
修复 1：HTML 标签剥离方式（错误→正确）
错误：keepChineseAndLetters()（已删除）只保留中文和英文字母，移除其他所有字符。HTML 属性中的英文字母（如 <p style="text-indent: 2em;"><strong> 中的 p、style、text-indent、em、strong）被保留，剥掉 < > 后形成垃圾字符串：

pstyletextindentemstrong编者按strongppstyletextindentemstrong癌症对人们

jieba 无法正确分词。

正确：新增 stripHtml() 和 decodeHtmlEntities()：

使用状态机完整移除 <...> HTML 标签（包括括号和属性）
解码 HTML 实体如 &lt; &gt; &amp; &quot; &nbsp;
保留所有文本字符、空格、标点，让 jieba 获得干净的输入
修复 2：_offsetLib 迭代顺序
问题：_offsetLib 声明为 unordered_map，导致第一次和第二次遍历的迭代顺序不同，dict[docId-1] 访问到错误的文档词频表。

修复：改为 map<int,pair<int,int>>，确保按 key (docId) 升序遍历。

修复 3：dict 数据结构（核心 Bug！）
问题：dict 是 vector<map<string,int>>，使用 push_back() 顺序存储。第一遍遍历中，如果某个文档 XML 解析失败、content 为空等情形会 continue 跳过 push_back()，导致 dict 索引与 docId 错位。第二遍用 dict[docId-1] 访问时拿到的是另一个文档的词频表，导致 docMap.find(t) 失败 → continue → 该文档的所有词都不进入倒排索引。

这解释了为什么3729 个唯一词对于 3791 个文档来说异常地少 — 大部分文档的词都被静默跳过了。

修复：将 dict 从 vector<map<string,int>> 改为 map<int,map<string,int>>，直接用 docId 做 key：

第一遍：dict[docId] = tempMap;
第二遍：dict.find(docId) 查找
修复 4：第一遍增加空值保护
第一遍遍历也增加了 contentNode 和 contentText 的空指针检查，与第二遍一致，防止潜在的段错误。